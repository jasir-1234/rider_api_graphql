# @format

type DeliverAddress {
  id: ID!
  name: String!
  address: String!
  message: String!
}

input Address{
  id: ID
  name: String!
  address: String!
  message: String!
}


type Mutation {
  createUser(name: String!, address: String!,message: String!): Address
}
